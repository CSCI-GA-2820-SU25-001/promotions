apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: promotions
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promotions
                port:
                  number: 80
